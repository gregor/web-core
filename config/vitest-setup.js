// Registers jest-dom's matchers (toBeInTheDocument, toHaveTextContent, ...) with
// vitest's expect. Wired in automatically by defineAppConfig, so apps need no setup
// file of their own.
import '@testing-library/jest-dom/vitest';
